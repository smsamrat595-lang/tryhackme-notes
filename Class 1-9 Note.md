# NRETWorking:
We can cheak IP address in - AbuseIPDB, Cisco, Talos, Intelligence
ip -  192. 168.  1.  1 (Privet ip)
      ⬇️   ⬇️  ⬇️  ⬇️
OCTAL #1    #2  #3   #4 (Every octal shold be from 0-255)

ip - 86.157.52.21 (Public IP)

# MAC - Media access Control
a4:c3:f0:84:ac:2d (MAC Address)
The 3 first section is the company address and last 3 is the unique address 

# We can cheak ping :
Command: ping -c {IP or URL}
If ip's last part is .1 or .254 it is the gateway(ROUTER)

Mac address is the digital fingerprint (It can not be changed bt anyone)
Ip is the logical Indentefier

# DHCP(Dynamic host Configuretion Protocol) Changes the ip adderss 

# OSI MODE: 
Open System Intereconnection model 
TCP - SLOW (It send small packet but this is good)
UDP - Fast (Some packet missing some time but this is fast) 

# From Packet and frame we shouls just know what is the source and what is the destination address 

# And the Last one is PORT NUMBER 
Port number should be (0 to 65535)
Common Port number (0-1024)

And some common port is:
Common Well-Known Ports (0-1023)
20 & 21: FTP (File Transfer Protocol) for file transfers.
22: SSH (Secure Shell) for secure remote login.
23: Telnet (legacy remote access).
25: SMTP (Simple Mail Transfer Protocol) for sending email.
53: DNS (Domain Name System) for name resolution.
80: HTTP (HyperText Transfer Protocol) for unencrypted web traffic.
110: POP3 (Post Office Protocol) for receiving email.
143: IMAP (Internet Message Access Protocol) for receiving email.
443: HTTPS (HTTP Secure) for encrypted web traffic. 
Other Frequently Used Ports
3306: MySQL Database.
3389: RDP (Remote Desktop Protocol). 
